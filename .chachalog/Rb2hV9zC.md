---
# Allowed version bumps: patch, minor, major
"@jahia/jcontent": minor
---

The preview now renders images in formats a browser cannot decode, such as TIFF and PSD, by showing a generated rendition instead of the original file, and falls back to a file icon when no rendition exists rather than showing a broken image.
