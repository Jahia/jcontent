---
# Allowed version bumps: patch, minor, major
"@jahia/jcontent": patch
---

A file uploaded from inside a picker is only selected if the picker accepts it: uploading a PDF from an image picker no longer leaves it selected with the select button enabled. Uploading an image still selects it, as before.
