---
# Allowed version bumps: patch, minor, major
"@jahia/jcontent": patch
---

Restore the "Add" insertion point and the right-click Create action on the children of a list whose view renders them itself, with no wildcard placeholder in the rendered HTML (#2771). Page Builder now falls back to the types the definition allows as unnamed children when the rendering carries no such placeholder.
