---
# Allowed version bumps: patch, minor, major
"@jahia/jcontent": patch
---

Upgrading jContent now removes the older visibility module by itself, so visibility conditions keep working without installing a separate migration module first (#2724)
