# DO NOT EDIT
# This file is managed globally at https://github.com/Jahia/.github
name: Chachalog - Prepare Changelog

on:
  push:
    branches:
      - main
      - master
      - [0-9]_[0-9]_x
  workflow_dispatch: null

jobs:
  prepare-changelog:
    permissions:
      contents: write
      pull-requests: write
    uses: Jahia/jahia-modules-action/.github/workflows/reusable-chachalog-prepare-changelog.yml@v2
    secrets: inherit
