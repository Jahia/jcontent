# editframe-styles

This package provides a scoped stylesheet ready for injection in the editframe. It contains:

- Styles for JContent UI in the editframe (e.g. Box and Boxes)
- The scoped CSS build of Moonstone

The JS entrypoint exposes the original classnames to hashed classnames mapping of the styles.
