package org.jahia.modules.contentmanager.actionlists;

import org.jahia.services.render.RenderContext;

public class SavedSearchActionListRenderer implements ActionListRenderer {
    @Override
    public String renderActionList(RenderContext renderContext) {

        // we will perform a JCR query to retrieve all the saved search nodes and then call the renderService
        // to render them with their custom views

        // @todo to be implemented

        return "";
    }
}
