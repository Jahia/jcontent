## Note on deployment
Currently, it is impossible to deploy this module along with the rest of pom-based modules as it doesn't pick up the .tgz files.
It is deployed manually from `/fixtures` instead.  
